const initialState = {
  bySizesPrice: [2500, 6000],
  // bySizes: ['Премиум'] //, 'Стандартный', 'Большой'
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}