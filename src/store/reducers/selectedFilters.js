const initialState = {
  // bySizesPrice: [2300, 70000],
  bySizes: ['Премиум', 'Стандартный']
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}